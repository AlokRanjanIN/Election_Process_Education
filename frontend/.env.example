# Frontend environment variables
# For local Vite development, this can stay empty because /api is proxied to http://localhost:8080.
# For production builds, set this to your deployed backend URL.

VITE_API_BASE_URL=
